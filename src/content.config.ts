/**
 * Astro content collections config.
 *
 * As of the Sanity migration, all site content lives in Sanity — this file no
 * longer defines any active collections. The old `experiences/` markdown files
 * are preserved on disk for git history but are not read by the site.
 *
 * If you re-enable a collection here later, restore the corresponding schema
 * from git history (see `src/content.config.ts` before the Sanity migration).
 */

export const collections = {};
