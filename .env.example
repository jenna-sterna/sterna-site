# Sanity CMS configuration.
# Copy this to `.env` and fill in the values. Defaults kick in if left blank.

SANITY_PROJECT_ID=jghpyuue
SANITY_DATASET=production

# Optional: only needed for previewing drafts. Get one at
# https://sanity.io/manage/project/jghpyuue/api → API tokens (Viewer role).
SANITY_READ_TOKEN=
